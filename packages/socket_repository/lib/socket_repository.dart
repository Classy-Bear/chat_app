/// This repository handles the socket events and callbacks of the app.
///
/// Pass the user id of the sender like this:
///
/// ```dart
/// final user = User(id: 'user-id', ...);
/// SocketRepository(user.id);
/// ```
library socket_repository;

export 'src/models/user.dart';
export 'src/models/user.dart';
export 'src/socket_repository.dart';
